// Copyright 2020 Amazon.com, Inc. or its affiliates. All Rights Reserved.
// SPDX-License-Identifier: Apache-2.0

package software.aws.toolkits.jetbrains.core.filtering

class ResourceFilterManagerTest {
    /*
        @Test
    fun `filtersEnabled returns true if tags specified and enabled`() {
        TagFilterStorage.getInstance(projectRule.project).loadState(
            listOf(
                TagFilterFilter(
            name="default",
            enabled = true,
            tagKey = "tag",
            tagValues = listOf()
                )       ))
        assertThat(filterManager().en).isTrue()
    }

    @Test
    fun `tagFiltersEnabled returns false when no tags specified`() {
        assertThat(filterManager().tagFiltersEnabled()).isFalse()
    }

     */
}
